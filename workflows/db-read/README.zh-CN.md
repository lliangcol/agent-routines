# db-read Workflow

## 目的

校验只读 SQL wrapper 输入，拒绝写入、DDL 和执行类关键字，不连接真实数据库。

## 支持系统

支持 Windows PowerShell 5.1/PowerShell 7+、macOS Bash/PowerShell 7+ 和 Linux Bash/PowerShell 7+。

## 说明

本文件是面向用户的中文 README companion；workflow 行为以脚本和 schema 为准。
