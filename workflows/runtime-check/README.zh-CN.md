# runtime-check Workflow

## 目的

检查本地 agent runtime 前置条件，例如 shell、PATH、package-manager 命令、用户/项目 runtime 设置和 hook wrapper 存在性。

## 边界

不修改运行时配置。

## 说明

本文件是面向用户的中文 README companion；workflow 行为以脚本和 schema 为准。
