# github-guard Checklist

- Inspect `.github/workflows` before recommending required checks.
- Prefer checks with local workflow evidence.
- Distinguish workflow file names, job ids, and GitHub-accepted check names.
- Keep ruleset enforcement conservative when check names are unverified.
- Do not save remote settings without explicit authorization.
- Report remote-only evidence gaps.
