# java-maven-verify

## 用途

用于目标 Maven 测试、模块级验证、依赖解析诊断、镜像覆盖审阅和 Windows PowerShell Maven 命令构造。

## 非适用

不用于依赖升级、生产 profile 执行，或没有明确批准的宽泛构建系统变更。

## 说明

本文件是面向用户的中文 README companion；agent 执行指令仍以 `SKILL.md` 为准。
