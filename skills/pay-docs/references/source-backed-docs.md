# source backed docs

文档必须区分三类信息：

- 源码已证实：能从代码路径、枚举、DTO、处理器或测试中直接确认。
- 配置已证实：能从配置文件、默认值、环境变量或配置中心样例中确认。
- 运行环境待确认：需要真实环境、Webhook 订阅、第三方后台或人工执行结果才能确认。

不要把推断写成事实。若源码只能证明存在处理路径，不能证明线上已订阅或已启用，应明确写成“待确认”。
