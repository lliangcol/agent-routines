# graph-audit

## 用途

用于 MCP 图就绪状态、codebase-memory 项目索引检查、图优先发现边界、全局与项目指令范围和 fallback 计划。

## 非适用

不用于注册 MCP server、索引大型仓库、修改全局配置，或不经验证依赖陈旧图数据。

## 说明

本文件是面向用户的中文 README companion；agent 执行指令仍以 `SKILL.md` 为准。
