# prompt-qa

## 用途

用于提示词改进、prompt-only review-fix-re-review、执行契约加固，以及把模糊指令转换成有边界的 agent 任务。

## 非适用

不执行提示词描述的 workflow，不把提示词目标中的仓库文件修改作为本技能自身结果，也不验证提示词里提到的外部系统。

## 说明

本文件是面向用户的中文 README companion；agent 执行指令仍以 `SKILL.md` 为准。
