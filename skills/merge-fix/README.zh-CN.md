# merge-fix

## 用途

用于 merge conflict、rebase conflict、生成文件冲突检查和 conflict marker 清理。

## 非适用

不用于机械选择 ours/theirs、在冲突处理时做无关重构，或未获用户确认就完成 merge。

## 说明

本文件是面向用户的中文 README companion；agent 执行指令仍以 `SKILL.md` 为准。
