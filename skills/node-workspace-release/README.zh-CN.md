# node-workspace-release

## 用途

用于 pnpm/npm workspace 验证、插件 marketplace 元数据检查、release dry-run、package scripts、schema/doc/runtime 验证。

## 非适用

不在没有明确批准时发布 package、改版本号或安装依赖。

## 说明

本文件是面向用户的中文 README companion；agent 执行指令仍以 `SKILL.md` 为准。
