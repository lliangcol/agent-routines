# windows profile

Windows profile: PowerShell foreach pipeline parsing, curl/wget aliases, Git Bash not on PATH, uv-managed Python, readonly registry/startup checks, Visual Studio Build Tools, and Windows SDK.