# runtime-repair

## 用途

用于 Agent CLI 启动失败、损坏 package-manager shim、hook stdin 编码问题、auto-update 状态审阅和版本分流。

## 非适用

不用于仓库业务逻辑变更、缺少证据的宽泛工具升级，或在失败层定位前编辑用户运行时配置。

## 说明

本文件是面向用户的中文 README companion；agent 执行指令仍以 `SKILL.md` 为准。
