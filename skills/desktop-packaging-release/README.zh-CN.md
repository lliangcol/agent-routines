# desktop-packaging-release

## 用途

用于 Electron Builder 或 Forge 配置、桌面包元数据、Windows/macOS/Linux 产物、签名边界、dry-run 打包和发布就绪审阅。

## 非适用

不在没有明确批准时发布、签名、打 tag 或上传产物。

## 说明

本文件是面向用户的中文 README companion；agent 执行指令仍以 `SKILL.md` 为准。
