# Skill Authoring Guide

Every Skill needs YAML frontmatter with `name`, `description`, and `os: cross-platform`. The description must say what the Skill does and when to use it. Keep Skill text tool-neutral where practical.

Body sections should cover operating system support, workflow routing, human confirmation gates, and failure routing. References hold detailed checklists and examples.