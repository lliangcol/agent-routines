# Naming

Skill names use lowercase kebab-case. Names must not contain `claude`, `anthropic`, XML tags, spaces, or uppercase letters. Workflow names also use lowercase kebab-case and match their folder names.

Use concise names that describe reusable behavior rather than one project or one operating system. OS-specific behavior belongs in references, profiles, or scripts, not in top-level Skill names.