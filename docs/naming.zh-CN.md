# 命名

Skill 名称使用小写 kebab-case。名称不得包含 `claude`、`anthropic`、XML 标签、空格或大写字母。Workflow 名称也使用小写 kebab-case，并与其目录名一致。

使用简洁名称描述可复用行为，而不是描述某个项目或某个操作系统。操作系统特定行为应放在 references、profiles 或 scripts 中，而不是放在顶层 Skill 名称中。
