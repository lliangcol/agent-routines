# Skill 编写指南

每个 Skill 都需要 YAML frontmatter，并包含 `name`、`description` 和 `os: cross-platform`。`description` 必须说明该 Skill 做什么以及何时使用。可行时，Skill 文本应保持工具中立。

正文部分应覆盖操作系统支持、workflow 路由、人工确认门禁和失败路由。References 用于存放详细清单和示例。
