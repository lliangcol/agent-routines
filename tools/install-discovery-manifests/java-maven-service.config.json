{
  "version": 2,
  "userTargets": {
    "enabled": false,
    "tools": ["codex", "claudeCode"],
    "skills": {
      "codex": [],
      "claudeCode": []
    },
    "workflows": []
  },
  "projectDefaults": {
    "enabled": true,
    "tools": ["codex", "claudeCode"],
    "skills": {
      "codex": ["guarded-change", "review-loop", "java-maven-verify", "security-review", "commit-guard"],
      "claudeCode": ["guarded-change", "review-loop", "java-maven-verify", "security-review", "commit-guard"]
    },
    "workflows": ["preflight", "gate-check", "maven-check", "security-check", "commit-check", "release-check"],
    "createTargets": false,
    "mode": "merge"
  },
  "projectTargets": [
    {
      "path": "REPLACE_WITH_JAVA_MAVEN_PROJECT_PATH",
      "enabled": true,
      "tools": ["codex", "claudeCode"],
      "skills": {
        "codex": ["guarded-change", "review-loop", "java-maven-verify", "security-review", "commit-guard"],
        "claudeCode": ["guarded-change", "review-loop", "java-maven-verify", "security-review", "commit-guard"]
      },
      "workflows": ["preflight", "gate-check", "maven-check", "security-check", "commit-check", "release-check"],
      "createTargets": false,
      "mode": "merge"
    }
  ],
  "discovery": {
    "roots": [],
    "maxDepth": 0,
    "excludeDirs": [".git", "node_modules", "vendor", "dist", "build", "target", ".tmp", ".cache", "tmp", "temp", ".agent-routines", ".codex", ".claude"],
    "skipNestedRepos": true,
    "rootOptions": []
  },
  "promotionRules": {
    "doNotPromoteToUserSkills": ["api-sync", "dms-repair", "electron-app-builder", "node-workspace-release", "pay-docs"]
  },
  "output": {
    "manifestPath": ".agent-routines/generated/java-maven-service.install.manifest.json",
    "reportPath": ".agent-routines/generated/java-maven-service.install.plan.json"
  },
  "applySafety": {
    "unknownInstalledItems": "report-only"
  }
}
