{
  "version": 2,
  "userTargets": {
    "enabled": false,
    "tools": ["codex", "claudeCode"],
    "skills": {
      "codex": [],
      "claudeCode": []
    },
    "workflows": []
  },
  "projectDefaults": {
    "enabled": true,
    "tools": ["codex", "claudeCode"],
    "skills": {
      "codex": ["guarded-change", "review-loop", "node-workspace-release", "security-review", "commit-guard"],
      "claudeCode": ["guarded-change", "review-loop", "node-workspace-release", "security-review", "commit-guard"]
    },
    "workflows": ["preflight", "gate-check", "node-workspace-check", "security-check", "commit-check"],
    "createTargets": false,
    "mode": "merge"
  },
  "projectTargets": [
    {
      "path": "REPLACE_WITH_NODE_WORKSPACE_PROJECT_PATH",
      "enabled": true,
      "tools": ["codex", "claudeCode"],
      "skills": {
        "codex": ["guarded-change", "review-loop", "node-workspace-release", "security-review", "commit-guard"],
        "claudeCode": ["guarded-change", "review-loop", "node-workspace-release", "security-review", "commit-guard"]
      },
      "workflows": ["preflight", "gate-check", "node-workspace-check", "security-check", "commit-check"],
      "createTargets": false,
      "mode": "merge"
    }
  ],
  "discovery": {
    "roots": [],
    "maxDepth": 0,
    "excludeDirs": [".git", "node_modules", "vendor", "dist", "build", "target", ".tmp", ".cache", "tmp", "temp", ".agent-routines", ".codex", ".claude"],
    "skipNestedRepos": true,
    "rootOptions": []
  },
  "promotionRules": {
    "doNotPromoteToUserSkills": ["api-sync", "dms-repair", "java-maven-verify", "pay-docs"]
  },
  "output": {
    "manifestPath": ".agent-routines/generated/node-workspace.install.manifest.json",
    "reportPath": ".agent-routines/generated/node-workspace.install.plan.json"
  },
  "applySafety": {
    "unknownInstalledItems": "report-only"
  }
}
