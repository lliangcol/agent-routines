{
  "version": 2,
  "userTargets": {
    "enabled": false,
    "tools": ["codex", "claudeCode"],
    "skills": {
      "codex": [],
      "claudeCode": []
    },
    "workflows": []
  },
  "projectDefaults": {
    "enabled": true,
    "tools": ["codex", "claudeCode"],
    "skills": {
      "codex": ["guarded-change", "env-audit", "runtime-repair", "security-review", "governance-audit"],
      "claudeCode": ["guarded-change", "env-audit", "runtime-repair", "security-review", "governance-audit"]
    },
    "workflows": ["preflight", "gate-check", "runtime-check", "security-check", "governance-check", "release-check", "startup-check"],
    "createTargets": false,
    "mode": "merge"
  },
  "projectTargets": [
    {
      "path": "REPLACE_WITH_WORKSTATION_CONFIG_PROJECT_PATH",
      "enabled": true,
      "tools": ["codex", "claudeCode"],
      "skills": {
        "codex": ["guarded-change", "env-audit", "runtime-repair", "security-review", "governance-audit"],
        "claudeCode": ["guarded-change", "env-audit", "runtime-repair", "security-review", "governance-audit"]
      },
      "workflows": ["preflight", "gate-check", "runtime-check", "security-check", "governance-check", "release-check", "startup-check"],
      "createTargets": false,
      "mode": "merge"
    }
  ],
  "discovery": {
    "roots": [],
    "maxDepth": 0,
    "excludeDirs": [".git", "node_modules", "vendor", "dist", "build", "target", ".tmp", ".cache", "tmp", "temp", ".agent-routines", ".codex", ".claude"],
    "skipNestedRepos": true,
    "rootOptions": []
  },
  "promotionRules": {
    "doNotPromoteToUserSkills": ["api-sync", "dms-repair", "electron-app-builder", "java-maven-verify", "node-workspace-release", "pay-docs"]
  },
  "output": {
    "manifestPath": ".agent-routines/generated/workstation-config.install.manifest.json",
    "reportPath": ".agent-routines/generated/workstation-config.install.plan.json"
  },
  "applySafety": {
    "unknownInstalledItems": "report-only"
  }
}
